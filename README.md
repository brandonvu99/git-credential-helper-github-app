Use as your git credential helper by running this command, replacing `<path_to_git-credential-helper-github-app.sh>`:

    git config --global credential.helper <path_to_git-credential-helper-github-app.sh>
